
import React from 'react';
import { Language } from '../types';

interface WelcomeScreenProps {
  onStart: (language: Language) => void;
  error: string | null;
}

const WelcomeScreen: React.FC<WelcomeScreenProps> = ({ onStart, error }) => {
  return (
    <div className="text-center bg-slate-800 p-8 rounded-2xl shadow-2xl transition-all duration-300 ease-in-out">
      <h1 className="text-4xl md:text-5xl font-bold text-cyan-300 mb-4">Multilingual Proficiency Test</h1>
      <h2 className="text-2xl md:text-3xl font-semibold text-white mb-6">Which language would you like to test?</h2>
      <p className="text-slate-300 mb-8 max-w-lg mx-auto">
        Discover your vocabulary proficiency! Each test consists of 30 multiple-choice questions
        generated by AI to estimate your level.
      </p>
      {error && (
        <div className="bg-red-500/20 border border-red-500 text-red-300 px-4 py-3 rounded-lg relative mb-6" role="alert">
          <strong className="font-bold">Error: </strong>
          <span className="block sm:inline">{error}</span>
        </div>
      )}
      <div className="flex flex-col md:flex-row gap-4 justify-center">
        <button
          onClick={() => onStart('hsk')}
          className="bg-cyan-500 text-slate-900 font-bold py-3 px-8 rounded-full text-lg hover:bg-cyan-400 transition-all duration-300 transform hover:scale-110 focus:outline-none focus:ring-4 focus:ring-cyan-300/50"
        >
          Chinese (HSK)
        </button>
        <button
          onClick={() => onStart('ielts')}
          className="bg-emerald-500 text-slate-900 font-bold py-3 px-8 rounded-full text-lg hover:bg-emerald-400 transition-all duration-300 transform hover:scale-110 focus:outline-none focus:ring-4 focus:ring-emerald-300/50"
        >
          English (IELTS)
        </button>
        <button
          onClick={() => onStart('dele')}
          className="bg-amber-500 text-slate-900 font-bold py-3 px-8 rounded-full text-lg hover:bg-amber-400 transition-all duration-300 transform hover:scale-110 focus:outline-none focus:ring-4 focus:ring-amber-300/50"
        >
          Spanish (DELE)
        </button>
      </div>
    </div>
  );
};

export default WelcomeScreen;
